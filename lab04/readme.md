Лабораторная работа №4: Настройка Jenkins для автоматизации DevOps-процессов (CI/CD)
Цель работы

Освоить базовую установку и конфигурацию Jenkins в Docker для автоматизации процессов DevOps, включая:

запуск контроллера Jenkins в контейнере;

настройку удалённого SSH-агента;

создание конвейера (Pipeline) для PHP-проекта с выполнением автоматических тестов.

Подготовка

Работа выполняется в каталоге lab04 репозитория GitHub. В этой директории должны храниться все необходимые файлы и конфигурации.
Для выполнения лабораторной работы необходимо наличие установленных Docker и Docker Compose.

Задача

Создать и настроить инфраструктуру, состоящую из двух контейнеров:

Jenkins Controller — основной управляющий сервер.

SSH Agent — удалённый агент, выполняющий сборку и тестирование.

Ход выполнения работы
Шаг 1. Создание структуры проекта

Создаём новую директорию и переходим в неё:

mkdir lab04
cd lab04

Шаг 2. Создание файла docker-compose.yml

Создаём файл docker-compose.yml и добавляем конфигурацию для Jenkins Controller:

version: '3.8'

services:
  jenkins-controller:
    image: jenkins/jenkins:lts
    container_name: jenkins-controller
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
    networks:
      - jenkins-network

volumes:
  jenkins_home:
  jenkins_agent_volume:

networks:
  jenkins-network:
    driver: bridge

Шаг 3. Запуск Jenkins Controller

Выполним запуск контейнера командой:

docker compose up -d


После запуска откроем браузер и перейдём по адресу:

👉 http://localhost:8080

Откроется страница начальной настройки Jenkins.

Шаг 4. Разблокировка и первичная настройка Jenkins

Чтобы войти в систему, нужно получить временный пароль администратора.
Для этого вводим команду:

docker exec -it jenkins-controller cat /var/jenkins_home/secrets/initialAdminPassword


Скопированный пароль вставляем на странице Unlock Jenkins.

Далее выбираем вариант установки плагинов:

Install suggested plugins

После завершения установки (примерно 2–5 минут) Jenkins предложит создать первого пользователя.
Заполняем форму, нажимаем Save and Continue, затем Start using Jenkins.
После этого откроется главная панель Jenkins.

Шаг 5. Подготовка SSH-агента

Теперь добавим второй контейнер — ssh-agent, который будет выполнять сборки через SSH.

5.1 Генерация SSH-ключей

Создаём директорию для ключей:

mkdir secrets
cd secrets
ssh-keygen -f jenkins_agent_ssh_key


В результате появятся два файла:

jenkins_agent_ssh_key — приватный ключ

jenkins_agent_ssh_key.pub — публичный ключ

5.2 Создание Dockerfile для агента

В корне lab04 создаём Dockerfile со следующим содержимым:

FROM jenkins/ssh-agent

RUN apt-get update \
 && apt-get install -y php-cli php-xml php-mbstring git curl unzip \
 && curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer \
 && rm -rf /var/lib/apt/lists/*


Этот Dockerfile настраивает образ на основе jenkins/ssh-agent и добавляет PHP и Composer для выполнения тестов.

5.3 Добавление ssh-agent в docker-compose.yml

Расширяем конфигурацию, добавив описание для ssh-агента:

  ssh-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ssh-agent
    environment:
      - JENKINS_AGENT_SSH_PUBKEY=${JENKINS_AGENT_SSH_PUBKEY}
    volumes:
      - jenkins_agent_volume:/home/jenkins/agent
    depends_on:
      - jenkins-controller
    networks:
      - jenkins-network

5.4 Создание файла .env

В корневом каталоге lab04 создаём файл .env и добавляем в него переменную окружения:

JENKINS_AGENT_SSH_PUBKEY=ssh-rsa AAAAB3... (содержимое файла jenkins_agent_ssh_key.pub)

5.5 Перезапуск Docker Compose

Применяем изменения:

docker compose up -d --build

Шаг 6. Настройка Jenkins для работы с SSH-агентом
6.1 Проверка плагина SSH Build Agents

В Jenkins переходим в:

Manage Jenkins → Plugins → Installed

Проверяем наличие плагина SSH Build Agents Plugin.
Если его нет, устанавливаем через вкладку Available.

6.2 Добавление SSH Credentials

Переходим в:

Manage Jenkins → Credentials → System → Global credentials

Добавляем новые учётные данные типа SSH Username with private key.
Заполняем:

Username: jenkins

Private Key: вставляем содержимое файла jenkins_agent_ssh_key

Passphrase: оставляем пустым

6.3 Создание нового узла (Node)

Переходим в:

Manage Jenkins → Nodes → New Node

Заполняем параметры:

Имя: ssh-agent1

Тип: Permanent Agent

Remote root directory: /home/jenkins/agent

Labels: php-agent

Executors: 1

Launch method: Launch agents via SSH

Host: ssh-agent

Credentials: выбираем созданные ранее SSH-ключи

После сохранения дождаться статуса Agent is online.

Шаг 7. Создание и настройка Pipeline для PHP-проекта
7.1 Размещение проекта

Для лабораторной работы использован PHP-проект recipe-book, размещённый на GitHub в репозитории iurii1801/auto_scripting, ветка lab04, каталог lab04/recipe-book.

7.2 Создание Jenkinsfile

В корне recipe-book создаём файл Jenkinsfile со следующим содержимым:

pipeline {
    agent {
        label 'php-agent'
    }

    stages {
        stage('Install Dependencies') {
            steps {
                echo 'Installing dependencies...'
                dir('lab04/recipe-book') {
                    sh 'composer install'
                }
            }
        }

        stage('Run Tests') {
            steps {
                dir('lab04/recipe-book') {
                    sh './vendor/bin/phpunit --testdox tests'
                }
            }
        }
    }

    post {
        always {
            echo 'Pipeline execution completed.'
        }
        success {
            echo 'Build finished successfully!'
        }
        failure {
            echo 'Build failed — please check logs.'
        }
    }
}

7.3 Настройка Pipeline в Jenkins

Создаём новый элемент:

New Item → php-lab04-pipeline → тип: Pipeline

В настройках выбираем:

Pipeline script from SCM

SCM: Git

Repository URL: https://github.com/iurii1801/auto_scripting.git

Branch Specifier: */lab04

Script Path: lab04/recipe-book/Jenkinsfile

Сохраняем и запускаем сборку.

7.4 Результат выполнения

В консоли выполнения появятся строки:

+ composer install
Installing dependencies...
+ ./vendor/bin/phpunit --testdox tests
PHPUnit 10.5.58 by Sebastian Bergmann and contributors.

Sample
 ✔ testTrue

Build finished successfully!
Finished: SUCCESS


Все этапы конвейера завершились без ошибок.

Контрольные вопросы

1. Какие преимущества даёт использование Jenkins в DevOps?
Jenkins обеспечивает автоматизацию процессов сборки, тестирования и доставки (CI/CD), минимизируя ручные действия и снижая вероятность ошибок. Его гибкая система плагинов позволяет интегрировать различные технологии и инструменты. Архитектура "controller–agent" даёт возможность масштабирования и разделения задач между разными узлами.

2. Какие существуют типы агентов в Jenkins?
Помимо SSH-агентов, Jenkins поддерживает:

JNLP-агенты — подключаются по протоколу JNLP, часто используются для Windows.

Docker-агенты — работают в изолированных контейнерах.

Kubernetes-агенты — создаются динамически в кластере Kubernetes.

Static agents — постоянные, вручную настроенные узлы.

Cloud agents — временные экземпляры, разворачиваемые в облаке.

3. С какими трудностями вы столкнулись и как их решили?
Возникали ошибки при подключении агента по SSH. Решением стало корректное создание и добавление SSH-ключей в Jenkins Credentials.
Также пришлось доустановить необходимые PHP-пакеты в контейнер агента и уточнить путь к Jenkinsfile в настройках Pipeline.
После исправлений система успешно выполнила сборку.

Выводы

В результате лабораторной работы была развернута и настроена система Jenkins для автоматизации CI/CD-процессов с использованием Docker.
Были изучены принципы работы контроллера и агента, реализовано подключение по SSH, создан и успешно выполнен Jenkins Pipeline для PHP-проекта.
Все этапы прошли успешно, что подтверждает корректность конфигурации и надёжность CI/CD-процесса.

Использованные источники

Jenkins Documentation — официальная документация по установке и настройке Jenkins.

Docker Hub Jenkins — официальный Docker-образ Jenkins.

SSH Build Agents Plugin — документация по подключению агентов по SSH.

Docker Compose Docs — руководство по многоконтейнерным приложениям.

Jenkins Pipeline Syntax — документация по синтаксису декларативных конвейеров.

PHP Official Docs — официальная документация PHP.

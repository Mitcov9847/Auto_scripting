# 🧩 Lab04 — Настройка Jenkins Controller и SSH Agent

## 📘 Цель работы

Цель лабораторной работы — научиться настраивать распределённую архитектуру Jenkins, в которой **Controller** управляет удалённым **Agent** через безопасное SSH-подключение.  
Это позволяет масштабировать систему сборок и разделять нагрузку между разными узлами.

---

## 🧠 Краткая теория

**Jenkins** — система непрерывной интеграции (CI), которая управляет процессами сборки, тестирования и развертывания приложений.

**Controller** — главный сервер, который:
- управляет задачами (Jobs),
- хранит конфигурации и плагины,
- распределяет сборки по агентам.

**Agent** — удалённый исполнитель задач, подключённый к Controller через SSH.

---

## ⚙️ Ход выполнения работы

### 1️⃣ Подготовка среды

Создал рабочую директорию проекта:

```bash
C:\Users\jenia\Desktop\AS_Croitor\lab04
В ней будут находиться все файлы Docker, ключи и отчёт README.md.

2️⃣ Генерация SSH-ключей
Сгенерировал SSH-ключи для безопасного соединения между Jenkins Controller и Agent:

powershell
Копировать код
ssh-keygen -t rsa -b 4096 -C "jenkins-agent"
После генерации появились файлы:

jenkins_agent_ssh_key — приватный ключ (используется контроллером),

jenkins_agent_ssh_key.pub — публичный ключ (копируется на агент).

3️⃣ Настройка прав доступа к приватному ключу
Установил права, чтобы только текущий пользователь имел доступ:

powershell
Копировать код
icacls jenkins_agent_ssh_key /grant:r "${env:USERNAME}:R"
4️⃣ Создание Dockerfile для Jenkins Controller
Создал Dockerfile для контроллера:

dockerfile
Копировать код
FROM jenkins/jenkins:lts
USER root
COPY jenkins_agent_ssh_key /var/jenkins_home/jenkins_agent_ssh_key
RUN chown jenkins:jenkins /var/jenkins_home/jenkins_agent_ssh_key && chmod 400 /var/jenkins_home/jenkins_agent_ssh_key
USER jenkins
Собрал и запустил контейнер:

bash
Копировать код
docker build -t lab04-jenkins .
docker run -d -p 8080:8080 -p 50000:50000 --name jenkins-controller lab04-jenkins
Jenkins теперь доступен по адресу:
👉 http://localhost:8080

5️⃣ Разблокировка Jenkins
Чтобы разблокировать Jenkins, получил пароль администратора:

bash
Копировать код
docker exec -it jenkins-controller cat /var/jenkins_home/secrets/initialAdminPassword
Скопировал пароль и вставил на странице Unlock Jenkins.

6️⃣ Установка плагинов и создание пользователя
Выбрал опцию Install suggested plugins.
После установки плагинов создал администратора с именем jenia.

7️⃣ Создание Dockerfile для Jenkins Agent
Создал второй Dockerfile для SSH-агента:

dockerfile
Копировать код
FROM jenkins/ssh-agent
COPY jenkins_agent_ssh_key.pub /home/jenkins/.ssh/authorized_keys
RUN chmod 600 /home/jenkins/.ssh/authorized_keys
Собрал и запустил контейнер агента:

bash
Копировать код
docker build -t lab04-ssh-agent .
docker run -d --name ssh-agent lab04-ssh-agent
8️⃣ Проверка контейнеров
Проверил, что оба контейнера запущены:

bash
Копировать код
docker ps
Ожидаемый результат:

bash
Копировать код
CONTAINER ID   IMAGE                 STATUS         PORTS
xxxxxxxxxxxx   lab04-ssh-agent       Up ...         22/tcp
xxxxxxxxxxxx   jenkins/jenkins:lts   Up ...         8080->8080, 50000->50000
9️⃣ Добавление нового узла в Jenkins
Открываю Manage Jenkins → Nodes → New Node

Ввожу имя узла, например agent

Выбираю Permanent Agent

Настраиваю параметры:

Remote root directory: /home/jenkins

Launch method: SSH

Host: ssh-agent

Credentials: выбираю SSH-ключ

Port: 22

Сохраняю изменения и проверяю статус подключения.

Если всё успешно — статус Connected ✅

🔎 10️⃣ Проверка SSH-подключения вручную
Для проверки подключаюсь к агенту напрямую:

bash
Копировать код
docker exec -it jenkins-controller bash
ssh -i /var/jenkins_home/jenkins_agent_ssh_key jenkins@ssh-agent
Если вижу приглашение jenkins@...$, значит соединение работает.

11️⃣ Тестовая задача в Jenkins
Создал новую задачу:

Имя: test-agent-job

Тип: Freestyle Project

В разделе Restrict where this project can be run указал agent

В Build добавил шаг:

bash
Копировать код
echo "Hello from Jenkins Agent"
После запуска получил результат:

vbnet
Копировать код
Hello from Jenkins Agent
Finished: SUCCESS
🖼️ Скриншоты (добавить после выполнения)
 Содержимое папки lab04

 Вывод docker ps

 Окно разблокировки Jenkins

 Настройка узла в интерфейсе

 Подключённый агент в Dashboard

 Успешная сборка Hello from Jenkins Agent

📚 Вывод по работе
В ходе лабораторной работы я:

Настроил Jenkins Controller и Agent в Docker;

Освоил подключение через SSH с использованием ключей;

Проверил соединение и выполнение задач на агенте;

Разобрал архитектуру распределённой CI/CD-системы.

Теперь я умею масштабировать Jenkins и подключать удалённые узлы для выполнения сборок.
